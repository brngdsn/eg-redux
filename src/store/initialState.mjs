const initialState = {
    colors: [
        { id: 0, color: `#000`, rating: 0 },
        { id: Math.random(), color: `#fff`, rating: 5 }
    ],
    sort: `NONE`
}

export default initialState